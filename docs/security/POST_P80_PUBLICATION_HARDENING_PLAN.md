# Post-P80 — Publication Hardening Plan

**Généré :** post-P80 Security Hardening  
**Date :** 2026-06-09  
**Mode :** SECURITY_HARDENING_CONTROLLED

---

## Résumé des actions de ce palier

| Blocker | Action | Statut |
|---|---|---|
| SEC-1 — Secret NEO4J | Plan de rotation documenté | DOCUMENTED — rotation externe requise |
| SEC-2 — CODEOWNERS absent | `.github/CODEOWNERS` créé | **RÉSOLU** |
| SEC-3 — dependabot absent | `.github/dependabot.yml` créé | **RÉSOLU** |
| SEC-4 — Branch protection | `docs/security/BRANCH_PROTECTION_POLICY.md` créé | DOCUMENTED — config GitHub manuelle |
| SEC-5 — Secret scan CI absent | `.github/workflows/secret-scan.yml` créé | **RÉSOLU** |
| SEC-6 — PROOFKIT gitignore | Entrée gitignore retirée, commentaire explicatif | **RÉSOLU** |

**Blockers restants après ce palier :**
- **SEC-1** : Rotation externe NEO4J requise — ne peut pas être faite automatiquement
- **SEC-4** : Configuration GitHub branch protection — requiert accès GitHub Settings (manuel)

---

## Détail des actions résolues

### SEC-2 — CODEOWNERS (RÉSOLU)

Fichier `.github/CODEOWNERS` créé avec protection :
- `sigma/`, `runtime_wiring/`, `connectors/` → @obsidia-kernel-team
- `proofs/lean/`, `proofs/merkle_root.json` → @obsidia-proof-team
- `.github/workflows/` → @obsidia-kernel-team

### SEC-3 — dependabot.yml (RÉSOLU)

Fichier `.github/dependabot.yml` créé :
- GitHub Actions : mise à jour hebdomadaire
- Python pip : commenté (à activer après création requirements.txt)

### SEC-5 — Secret scan CI (RÉSOLU)

Workflow `.github/workflows/secret-scan.yml` créé :
- TruffleHog OSS : scan sur push/PR vers main
- Gitleaks : scan complémentaire
- Se déclenche uniquement sur `main` — pas de spam sur branches d'audit

### SEC-6 — PROOFKIT_REPORT.json gitignore (RÉSOLU)

- Fichier `proofs/PROOFKIT_REPORT.json` : **IS TRACKED** par git (`git ls-files` confirmé)
- Gitignore avait une entrée incorrecte ("Generated by proofs/verify_all.py")
- **Action** : entrée retirée du `.gitignore`, commentaire explicatif ajouté
- **Décision** : `proofs/PROOFKIT_REPORT.json` est intentionnellement tracké — sert d'ancre RSSI interne (P79 `INCLUDE_INTERNAL_RSSI_ONLY`)

---

## Blockers restants

### SEC-1 — Rotation secret (EXTERNE)

Voir `docs/security/POST_P80_SECRET_ROTATION_PLAN.md`

- Décision : `PUBLICATION_BLOCKED_UNTIL_SECRET_ROTATED_EXTERNALLY`
- Fichier concerné : `docs/runtime/archive/.../BRODY_PHASE12E4_A2_DOMAIN_RACCORD_AUDIT_20260527.md`
- Zone : DO_NOT_PUBLISH — ne sera jamais pushé tel quel
- Action : rotation manuelle externe + redaction optionnelle du fichier archive

### SEC-4 — Branch protection (CONFIGURATION MANUELLE)

Voir `docs/security/BRANCH_PROTECTION_POLICY.md`

Configuration à réaliser via **GitHub Settings → Branches** avant publication :
- PR obligatoire, CI obligatoire, CODEOWNERS actif, no force push

---

## Conditions restantes pour publication GitHub

| Condition | Statut |
|---|---|
| CODEOWNERS | ✓ CRÉÉ |
| dependabot | ✓ CRÉÉ |
| Secret scan CI | ✓ CRÉÉ |
| Gitignore PROOFKIT | ✓ RÉSOLU |
| Branch protection | ⚠ MANUEL — config GitHub Settings |
| Secret rotation NEO4J | ⚠ EXTERNE — rotation manuelle requise |
| requirements.txt | ⚠ À CRÉER (optionnel pour prochaine étape) |
| node_modules check | ⚠ À VÉRIFIER (`git ls-files apps/...`) |

**Publication autorisée après :** SEC-1 rotation + SEC-4 configuration branch protection.

---

## Vérifications post-hardening

```bash
python proofs/verify_all.py           # PASS attendu
python scripts/check_forbidden_content.py   # PASS attendu
pytest tests/test_post_p80_publication_hardening.py -q  # PASS attendu
```

---

**Verdict :** `POST_P80_SECRET_ROTATION_PUBLICATION_HARDENING_READY`  
**Publication :** `STILL_BLOCKED_UNTIL_SECRET_ROTATED_EXTERNALLY`
