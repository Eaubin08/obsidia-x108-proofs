
proofs/lean/.lake/

__pycache__/
*.pyc
*.pyo

artifacts/

# --- local upstream workdirs ---
.cache/upstream/

# --- secrets / env ---
.env
.env.*
!.env.example

# --- runtime / logs ---
*.log
.runtime_logs/
**/__pycache__/
*.pyc

# --- heavy local artifacts ---
artifacts/**/raw/
artifacts/**/tmp/
artifacts/**/payloads/
artifacts/**/exports/
artifacts/**/db_dumps/

# --- node / python local deps ---
node_modules/
.venv/
venv/

# --- local snapshot workdirs ---
upstream_snapshots/
# --- local-only workdirs ---
.local/
staging/final_graft_plan/
staging/transplant_mapped/
staging/transplant_priority/

_external_sources/
MonProjet/allData/*.json
MonProjet/input_temp.json

# === OBSIDIA PUBLIC HYGIENE ===

# Private / staging / external source snapshots
private/
_imports_ready/
MonProjet/
_audit_tags/

# Local audit backups / temporary repair files
audit/backups/
audit/cleanup/secret_fix_backup_*/
*.bak
*.backup
*.before-*.py
*_backup*
_patch_*.py

# Build artifacts
bin/
obj/
*.dll
*.exe
*.pdb
*.db
*.sqlite
*.sqlite3

# Runtime/generated data
allData/
MonProjet/allData/
decision_*.json
!runtime_contracts/schemas/decision_ticket.schema.json
*_results.tmp

# Python / test cache
__pycache__/
.pytest_cache/
*.pyc

# External benchmarks (heavy, not part of source tree)
_external_benchmarks/
_external_benchmarks_wsl/

# Node / package artifacts
node_modules/
dist/
build/

# Env/secrets
.env
.env.*
!.env.example
audit/local_diag/
audit/local_rescue/
RECUPE_SCORING/

# Python cache

# Local audit outputs
audit_terrain/

# Runtime logs

# Local-only non-canonical holds
_NONCANONICAL_HOLD*/
.cache/
**/__pycache__/
*.pyc
*.pyo

# IDE
.idea/
.vscode/

# proofs/PROOFKIT_REPORT.json est intentionnellement traque (git-tracked)
# Il sert d ancre de reference pour l audit RSSI (P79 INCLUDE_INTERNAL_RSSI_ONLY)
# Resolution inconsistance gitignore post-P80 : entree retiree


# === SESSION STATE / ARCHIVE LOCALE ===
# Fichiers de session Brody, packets d'audit, archives lourdes
_sessions/
CURRENT_BRODY_*
CURRENT_GRAPHITI_*
CURRENT_MEMORY_*
CURRENT_X108_*
CURRENT_REPO_*

# Fichiers temporaires de scan/tri
_orphan_scan_*.txt
_orphan_scan_raw.txt
_phase1_live_retest.json
_scan_v5_err.txt
_utf8_livecheck_output.txt
TRI_RAPPORT_*.md
DEEPSEEK_*.md

# Données lourdes / patches non-source
GAVAGE_*.json
OBSIDIA_X108_FULL_STACK_*.zip
OBSIDIA_X108_V5A_*.zip
OBSIDIA_GENCOIN_*.zip
OBSIDIA_CONV_*.zip
Demo-obsidia-x108-proof-main*.zip
final_dump.json
root_fake_packet.json
force_live.json
raw_response.txt

# Manifests récursifs générés (trop lourds pour git)
MANIFEST_SHA256_RECURSIVE.json
MANIFEST_SHA256_RECURSIVE_ROOT.txt

# Chemin Windows leaké
C/

# === LOCAL / HEAVY / SESSION ARTIFACTS ===
GAVAGE_REALISE_ACCORD.json
OBSIDIA_X108_FULL_STACK_V3_V4_PLUS_WORLD_GATEWAY_PATCH.zip
*.zip
*.7z
*.rar
*.tar
*.gz
_sessions/
CURRENT_BRODY_*
CURRENT_GRAPHITI_*
CURRENT_MEMORY_*
CURRENT_X108_*

# === LOCAL AUDITS / GENERATED HUGE OUTPUTS ===
_local_audits/LOCATE_EXISTING_BRODY_CHAT_SURFACE/
_local_audits/brody_sessions/
*.zip
*.7z
*.rar
*.tar
*.gz
GAVAGE_REALISE_ACCORD.json
OBSIDIA_X108_FULL_STACK_V3_V4_PLUS_WORLD_GATEWAY_PATCH.zip

# === LOCAL SESSION PACKETS / ROOT STATE FILES ===
CURRENT_*
CURRENT_PUSH_CLEAN_VALIDATED.md
_sessions/
_local_audits/
audit_logs/
*.zip
*.7z
*.rar
*.tar
*.gz
GAVAGE_REALISE_ACCORD.json
OBSIDIA_X108_FULL_STACK_V3_V4_PLUS_WORLD_GATEWAY_PATCH.zip

# === LOCAL SENSITIVE PROBES ===
test_neo4j.py
*_local_password*
*.local.secret

# === LOCAL PRIVATE DOCS ===
*.docx

# === CI FREEZE POINTER — must be committed for CI test coverage ===
!CURRENT_BRODY_SIGMA_READONLY_CHAIN_F68.txt

# === OBSIDIA LOCAL-ONLY GENERATED ARTIFACTS (OBSIDIA_LOCAL_ONLY_GITIGNORE_GUARD_V0) ===
# These are local-only working directories — never push to GitHub.
# DO NOT add _FREEZE/, docs/, apps/, tests/, scripts/, periphery/,
# domains/, domain_packets/, registries/, schemas/ here — they contain
# functional files or proof freezes that may be committed.

# Patch proposal system (auto-generated by Claude Code)
_PATCH_PROPOSALS/

# Internal working freezes (archives de travail locales, phases p30-p55)
_freezes/

# Temporary core import staging area
_tmp_core_import/

# Backup files with underscore suffix (*.bak_<label>_<date>)
*.bak_*

# Ephemeral code sandboxes (auto-generated by Claude Code)
_EPHEMERAL_CODE_SANDBOX_*/

# Local audit working directories (dot-prefix hidden dirs)
.local_audits/
.local_exports/
.local_freezes/
.local_reports/
.local_test_runs/
.runtime_freezes/

# Local-prefixed audit and report outputs
LOCAL_AUDIT_*/
LOCAL_READ_*/
LOCAL_ONLY_*/

# Root-level local audit text files
AUDIT_*.txt
LOCAL_*.txt

# Root-level local audit directories (e.g. AUDIT_ALL_20260617_185205/)
AUDIT_ALL_*/
SOURCE_PACKS_DEEP_DIFF_AUDIT_*/

# Root-level local-only files (non-directory, e.g. LOCAL_ONLY_MATTER_RECONCILIATION_*.md)
LOCAL_ONLY_*

# Source discovery scripts (local discovery tooling)
_source_discovery/

# === OBSIDIA OLD-FREEZE LOCAL RESIDUES (PASS 2) ===
# Local-only debug session artifacts from old freeze sprints.
# DO NOT add _FREEZE/, docs/, reports/, proofs/, registries/ here.
# DO NOT add run_obsidure_*.py, run_all_lean_tests.py, run_lyapunov.py here.

# One-shot debug probes, retests, rootcause scripts
_probe_*.py
_retest_*.py
_rootcause_*.py
_tmp_*.py
_check_*.py
_mm_audit*.py

# Sensitive session dump files
_sec*.json
_status_*.txt
_obsidure_*_context.txt

# Runtime preflight working directory (P8-P56 phase debug artifacts)
_runtime_wiring_preflight/

# Root-level local text outputs from analysis passes
BALANCE_*.txt
FAST_PATH_EXTRACT_*.txt
GITHUB_*.txt

# Old component analysis outputs (superseded by agents registries)
OBSIDIA_COMPONENT_*/
OBSIDIA_COMPONENT_*.csv
OBSIDIA_CANONICAL_COMPONENT_CORPUS_MAP_*.csv

# Local verify output files
.local_verify_*

# Ephemeral sandbox build logs nested inside any directory (e.g. _FREEZE/)
# Root-level _EPHEMERAL_CODE_SANDBOX_*/ already covered above (PASS 1).
# This rule covers the same directories at any depth (Lean build iterations).
**/_EPHEMERAL_CODE_SANDBOX_*/
**/_EPHEMERAL_CODE_SANDBOX_*/*.log
.claude/hooks/.last_router_verdict.json
_MEMORY_INTAKE_OUTBOX/
_PATCH_PROPOSALS/
