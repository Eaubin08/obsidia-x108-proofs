# Path Compute V0

Status: advisory-only, non-sovereign, non-runtime.

```text
decision_authority = KX108_ONLY
path_compute_authority = NONE
runtime_enabled = false
```

This package does not authorize, execute, mutate, write memory, call MCP, bind Graphiti/Neo4j, or emit ALLOW/HOLD/BLOCK.
